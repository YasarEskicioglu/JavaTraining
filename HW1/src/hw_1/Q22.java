package hw_1;

public  Q22 {					
	/* Compile error occurs.
	 * Because there is no class definition in this line.  */
	
	double int count;	
	/* Compile error occurs.
	 * Because there are more than one primitive data type in same line. */
	void errors( ) {}
	// No compile error occurs.

	static void private limit; 
	/* Compile error occurs. 
	 * Because access modifiers always should always start at the beginning of the line. */

}
